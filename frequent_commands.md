## checking npu utilization real time like htop for cpu
- hailortcli monitor (runs only if the inference code is started with `env HAILO_MONITOR=1`)

## checking camera is detected or not
- rpicam-still --list-cameras